export interface MostUsedService {
    wrapper_name: String,
    count: String
  }

  export interface RecentlyUserService {
    invocation_time: any,
    wrapper_name: String
  }
  
  export interface TotalRequest {
    count: String
  }