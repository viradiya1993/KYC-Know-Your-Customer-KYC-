
import { GoLivePopupComponent } from './go-live-popup.component';

describe('GoLivePopupComponent', () => {
  let component: GoLivePopupComponent;

  beforeEach(async () => {
    component = new GoLivePopupComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
