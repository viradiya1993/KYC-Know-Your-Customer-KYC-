
import { UserServicePopupComponent } from './user-service-popup.component';

describe('UserServicePopupComponent', () => {
  let component: UserServicePopupComponent;

  beforeEach(async () => {
    component = new UserServicePopupComponent();
  });


  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
